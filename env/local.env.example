# =============================================================================
# CareCircle - Local Development Profile (TEMPLATE)
# =============================================================================
# Local services: Postgres, Redis, RabbitMQ via docker-compose
# These are the default values - safe to use as-is for local development.
#
# Copy to env/local.env (or use directly if values match your setup)
# =============================================================================

# =============================================================================
# DATABASE - PostgreSQL (Local Docker)
# =============================================================================
# Default docker-compose credentials
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=1234
DB_DATABASE=carecircle
DB_SSL=false

# Prisma connection string (must match above)
DATABASE_URL=postgresql://postgres:1234@localhost:5432/carecircle

# =============================================================================
# REDIS (Local Docker)
# =============================================================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_TLS=false
# NOTE: Don't set REDIS_URL for local - use host/port directly

# =============================================================================
# MESSAGE QUEUE - RabbitMQ (Local Docker)
# =============================================================================
AMQP_URL=amqp://guest:guest@localhost:5672
AMQP_HOST=localhost
AMQP_USER=guest
AMQP_PASSWORD=guest
AMQP_VHOST=/
AMQP_PORT=5672
AMQP_TLS=false

# =============================================================================
# MQTT (Local Docker via RabbitMQ)
# =============================================================================
MQTT_HOST=localhost
MQTT_PORT=1883
MQTT_USERNAME=guest
MQTT_PASSWORD=guest
MQTT_TLS=false

# =============================================================================
# WORKERS CONFIGURATION
# =============================================================================
WORKER_CONCURRENCY=10
JOB_ATTEMPTS=5
JOB_BACKOFF_DELAY=1000
JOB_TIMEOUT=30000
HEALTH_PORT=4001

# =============================================================================
# LOGGING
# =============================================================================
LOG_LEVEL=debug
PRISMA_LOG_LEVEL=error,warn
PRISMA_SLOW_QUERY_LOG=true

# =============================================================================
# OPTIONAL SERVICES (uncomment and configure as needed)
# =============================================================================

# Email (Production SMTP)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=your-sendgrid-api-key
# EMAIL_FROM=noreply@carecircle.com
# EMAIL_FROM_NAME=CareCircle

# SMS (Twilio)
# TWILIO_ACCOUNT_SID=your-account-sid
# TWILIO_AUTH_TOKEN=your-auth-token
# TWILIO_PHONE_NUMBER=+1234567890

# Alerting (Slack DLQ Webhook)
# SLACK_DLQ_WEBHOOK=https://hooks.slack.com/services/xxx/xxx/xxx

